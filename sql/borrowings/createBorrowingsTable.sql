CREATE TABLE IF NOT EXISTS borrowings (id INT AUTO_INCREMENT PRIMARY KEY, userId INT NOT NULL, bookId INT NOT NULL, borrowedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP, returnedAt TIMESTAMP NULL, FOREIGN KEY (userId) REFERENCES users(id) ON DELETE CASCADE, FOREIGN KEY (bookId) REFERENCES books(id) ON DELETE CASCADE);